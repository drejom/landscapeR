% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/17-project.R
\name{project_into}
\alias{project_into}
\title{Project a secondary cohort into the state-space of a primary cohort}
\usage{
project_into(
  std_primary,
  std_secondary,
  layer_primary = 1L,
  layer_secondary = 1L
)
}
\arguments{
\item{std_primary}{\code{StateTransitionData} with \code{metadata()$stage1}
present — supplies the \eqn{V^*} loadings.}

\item{std_secondary}{\code{StateTransitionData} to project.  Must share the
same feature space (genes) as the primary.  Only the first layer is used.}

\item{layer_primary}{integer — which layer of the primary object provides
the \eqn{V^*} loadings (default 1).}

\item{layer_secondary}{integer — which layer of the secondary object to
project (default 1).}
}
\value{
\code{std_secondary} with \code{metadata()$stage1} written.
}
\description{
Implements the Rockne2020 validation projection:
\deqn{X_{\rm secondary} \cdot V^*_{\rm primary}}
which places secondary samples on the same coordinate axes as the primary,
without re-fitting the decomposition.
}
\details{
The returned object is \code{std_secondary} augmented with
\code{metadata()$stage1} containing projected coordinates.  The primary
object is not modified.
}
