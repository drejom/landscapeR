% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/23a-stage1-targets-orchestration.R
\name{stage1_evidence_targets}
\alias{stage1_evidence_targets}
\title{Declare the durable Stage 1 full-evidence targets graph}
\usage{
stage1_evidence_targets(artifact_root, controller = "stage1-evidence")
}
\arguments{
\item{artifact_root}{absolute shared or controller-local directory in which
the controlling process atomically publishes the verified artifact.}

\item{controller}{name of the crew controller assigned to worker targets.}
}
\value{
a list of \code{targets} target objects suitable as the final expression
of a targets script.
}
\description{
This graph branches over the frozen benchmark task manifest, delegates those
branches to a named crew controller, reruns only invalid or failed branches,
and publishes a content-addressed artifact on the controlling process only
after all scientific dependencies complete. The manifest and its seed table
remain the authoritative RNG contract.
}
\details{
Use this function as the final expression in a targets script. Configure a
matching controller with \code{targets::tar_option_set(controller = controller)}
and run with \code{targets::tar_make(use_crew = TRUE)}. Replacing a local
controller with a scheduler controller does not change the scientific graph.
}
