% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13-stage0-synthetic.R
\name{synthetic_potential_control}
\alias{synthetic_potential_control}
\title{Generate a synthetic potential control via Langevin simulation}
\usage{
synthetic_potential_control(
  n = 100L,
  beta = 2,
  n_steps = 5000L,
  dt = 0.01,
  seed = 42L
)
}
\arguments{
\item{n}{integer number of samples to collect}

\item{beta}{numeric inverse temperature (higher = sharper wells, default 2)}

\item{n_steps}{integer Euler-Maruyama steps per sample (default 5000)}

\item{dt}{numeric step size (default 0.01)}

\item{seed}{integer RNG seed}
}
\value{
\code{StateTransitionData} with \code{PotentialGroundTruth} and
\code{metadata()$potential_control} carrying generating parameters.
The single experiment "coords" has one feature row: the simulated x values.
\code{colData} carries \code{x_coord} and \code{well} ("left"/"right").
}
\description{
Simulates samples from the double-well potential \eqn{U(x) = (x^2 - 1)^2}
using Euler-Maruyama integration of the overdamped Langevin equation
\eqn{dX_t = -U'(X_t) dt + \sqrt{2\beta^{-1}} dW_t}.
Samples are wrapped as a \code{StateTransitionData} with a
\code{PotentialGroundTruth} recording the known well locations,
barrier position, and barrier height.
}
\details{
The ground truth for this potential:
\itemize{
\item Wells at x = ±1 (minima of U)
\item Barrier at x = 0 (maximum of U between wells)
\item Barrier height = U(0) - U(1) = 1
}
}
