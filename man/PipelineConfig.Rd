% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-config.R
\name{PipelineConfig}
\alias{PipelineConfig}
\title{Construct a pipeline configuration}
\usage{
PipelineConfig(dataset, analysis, strategies = list(), params = list())
}
\arguments{
\item{dataset}{one non-empty dataset identifier}

\item{analysis}{a validated \code{\linkS4class{AnalysisSpecification}}}

\item{strategies}{named list mapping contract names to registered strategy
names}

\item{params}{named list mapping strategy names to parameter lists}
}
\value{
a validated \code{PipelineConfig}
}
\description{
This is the supported construction boundary for a reproducible pipeline
run. It avoids direct S4 slot assembly while preserving the configuration as
an inspectable value object.
}
\examples{
cfg <- PipelineConfig(
    dataset = "example",
    analysis = analysis_specification(
        id = "example-target",
        target_field = "condition",
        target_type = "binary",
        reference_level = "control",
        comparison_level = "treated"
    ),
    strategies = list(Decomposer = "svd"),
    params = list(svd = list())
)
}
