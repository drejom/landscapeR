% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13h-stage0-aml-control.R
\name{synthetic_k1_aml_longitudinal_control}
\alias{synthetic_k1_aml_longitudinal_control}
\title{Generate an AML-shaped repeated-subject K=1 calibration control}
\usage{
synthetic_k1_aml_longitudinal_control(
  subjects_per_condition = 12L,
  times = .aml_k1_default_times(),
  p = 100L,
  noise_sd = 0.03,
  time_signal = 8,
  disease_signal = 3,
  dropout_subjects = character(),
  removed_observations = data.frame(mouse_id = character(), weeks = numeric(), reason =
    character(), stringsAsFactors = FALSE),
  seed = 42L
)
}
\arguments{
\item{subjects_per_condition}{number of synthetic mice per condition}

\item{times}{irregular collection times in AML weeks}

\item{p}{number of expression features}

\item{noise_sd}{expression noise standard deviation}

\item{time_signal}{singular-score scale for the dominant time axis}

\item{disease_signal}{singular-score scale for the non-dominant target axis}

\item{dropout_subjects}{optional subject IDs whose final observation is
removed}

\item{removed_observations}{optional data frame with \code{mouse_id}, \code{weeks},
and \code{reason} columns declaring exact observations removed before
expression is generated; this supports auditable incomplete-trajectory
calibration without imputing or reclassifying observations}

\item{seed}{reproducibility seed}
}
\value{
a repeated-subject \code{StateTransitionData} with
\code{SubspaceGroundTruth} and AML control metadata
}
\description{
The control has repeated synthetic mice in CTL and disease groups.  Its
first planted sample axis is a stronger AML-informed collection-time
confounder, while its second planted axis is a non-dominant condition-by-time
disease divergence.  Times are the irregular week values used by the
packaged GSE133642 sample-weeks mapping.  The resulting object is intended
for disclosed calibration only; it carries truth and provenance but no
scientific acceptance judgement.
}
